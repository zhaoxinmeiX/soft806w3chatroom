version: 2.1

jobs:
  build-and-test:
  docker:
      - image: cimg/python:3.11

    steps:
      - checkout
      - run:
          name: build
          command: |
            docker build -t django-app .

workflows:
  django-ci:
    jobs:
      - build-and-test
